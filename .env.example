# Lyra runtime configuration lives in /lyra_data/config/lyra.toml.
# Start from lyra.toml.example instead of configuring the app with env vars.
#
# Secret values should be written to files under /lyra_data/secrets and
# referenced from lyra.toml with password_file, api_key_file, and
# service_account_file.
