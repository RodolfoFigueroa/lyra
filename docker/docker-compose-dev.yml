services:
  lyra:
    image: lyra:latest
    restart: unless-stopped
    ports:
      - 5219:5219
    volumes:
      - ${SERVICE_ACCOUNT_BIND_PATH}:/app/service-account.json:ro
      - ${LYRA_CACHE_BIND_PATH}:/lyra_cache
      - lyra_plugins:/lyra_plugins
    env_file:
      - ../.env
    build: ..
    develop:
      watch:
        - action: sync+restart
          path: ..
          target: /app
          ignore:
            - .git/
            - __pycache__/
            - .venv/
            - venv/
            - "*.env"
            - "*.ipynb"
        

  redis:
    image: redis:alpine
    restart: unless-stopped


  celery_worker:
    image: lyra:latest
    command: uv run --no-dev --frozen celery -A lyra.worker.celery_app worker --loglevel=info
    environment:
      LYRA_PLUGIN_MODE: worker
    volumes:
      - ${SERVICE_ACCOUNT_BIND_PATH}:/app/service-account.json:ro
      - ${LYRA_CACHE_BIND_PATH}:/lyra_cache
      - lyra_plugins:/lyra_plugins
    depends_on:
      - redis
      - lyra
    env_file:
      - ../.env
    build: ..
    develop:
      watch:
        - action: sync+restart
          path: ..
          target: /app
          ignore:
            - .git/
            - __pycache__/
            - .venv/
            - venv/
            - "*.env"
            - "*.ipynb"
networks:
  lyra_net:
    name: lyra_net
    driver: bridge

volumes:
  lyra_plugins:
    name: lyra_plugins