{
  "name": "lyra-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:api": "uv run --frozen --group dev python scripts/generate_api_docs.py",
    "predev": "npm run generate:api",
    "dev": "astro dev",
    "prebuild": "npm run generate:api",
    "build": "astro build",
    "precheck": "npm run generate:api",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.0",
    "astro": "^7.0.2",
    "starlight-versions": "^0.9.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.2",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "yaml": "^2.9.0"
  }
}
